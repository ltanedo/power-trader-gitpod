"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.when = void 0;
const expr_1 = require("./expr");
const polars_internal_1 = __importDefault(require("../internals/polars_internal"));
function WhenThenThen(_when) {
    return {
        when: ({ _expr }) => WhenThenThen(polars_internal_1.default._whenthenthen.when({ _when, _expr })),
        then: ({ _expr }) => WhenThenThen(polars_internal_1.default._whenthenthen.then({ _when, _expr })),
        otherwise: ({ _expr }) => (0, expr_1.Expr)(polars_internal_1.default._whenthenthen.otherwise({ _when, _expr }))
    };
}
function WhenThen(_when) {
    return {
        when: ({ _expr }) => WhenThenThen(polars_internal_1.default._whenthen.when({ _when, _expr })),
        otherwise: ({ _expr }) => (0, expr_1.Expr)(polars_internal_1.default._whenthen.otherwise({ _when, _expr }))
    };
}
/**
 * Utility function.
 * @see {@link when}
 */
function When(_when) {
    return {
        then: ({ _expr }) => WhenThen(polars_internal_1.default._when.then({ _when, _expr }))
    };
}
/**
 * Start a when, then, otherwise expression.
 *
 * @example
 * ```
 * // Below we add a column with the value 1, where column "foo" > 2 and the value -1 where it isn't.
 * >>> df = pl.DataFrame({"foo": [1, 3, 4], "bar": [3, 4, 0]})
 * >>> df.withColumn(pl.when(pl.col("foo").gt(2)).then(pl.lit(1)).otherwise(pl.lit(-1)))
 * shape: (3, 3)
 * ┌─────┬─────┬─────────┐
 * │ foo ┆ bar ┆ literal │
 * │ --- ┆ --- ┆ ---     │
 * │ i64 ┆ i64 ┆ i32     │
 * ╞═════╪═════╪═════════╡
 * │ 1   ┆ 3   ┆ -1      │
 * ├╌╌╌╌╌┼╌╌╌╌╌┼╌╌╌╌╌╌╌╌╌┤
 * │ 3   ┆ 4   ┆ 1       │
 * ├╌╌╌╌╌┼╌╌╌╌╌┼╌╌╌╌╌╌╌╌╌┤
 * │ 4   ┆ 0   ┆ 1       │
 * └─────┴─────┴─────────┘
 *
 * // Or with multiple `when, thens` chained:
 * >>> df.with_column(
 * ...     pl.when(pl.col("foo").gt(2))
 * ...     .then(1)
 * ...     .when(pl.col("bar").gt(2))
 * ...     .then(4)
 * ...     .otherwise(-1)
 * ... )
 * shape: (3, 3)
 * ┌─────┬─────┬─────────┐
 * │ foo ┆ bar ┆ literal │
 * │ --- ┆ --- ┆ ---     │
 * │ i64 ┆ i64 ┆ i32     │
 * ╞═════╪═════╪═════════╡
 * │ 1   ┆ 3   ┆ 4       │
 * ├╌╌╌╌╌┼╌╌╌╌╌┼╌╌╌╌╌╌╌╌╌┤
 * │ 3   ┆ 4   ┆ 1       │
 * ├╌╌╌╌╌┼╌╌╌╌╌┼╌╌╌╌╌╌╌╌╌┤
 * │ 4   ┆ 0   ┆ 1       │
 * └─────┴─────┴─────────┘
 * ```
 */
function when(expr) {
    return When(polars_internal_1.default.when(expr));
}
exports.when = when;
